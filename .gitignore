# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
build/
dist/
.pytest_cache/
.ruff_cache/
.ipynb_checkpoints/

# Data — raw + processed are regenerated by `python data/fetch_data.py` and
# `python -m analytics.build`. NOT committed (raw ~200MB). The small curated
# artifacts the dashboard needs (web/public/data/*) ARE committed on purpose.
data/raw/
data/processed/

# Node / Next.js (web/)
node_modules/
.next/
web/next-env.d.ts
web/.vercel/
.env
.env*.local

# OS
.DS_Store
Thumbs.db
